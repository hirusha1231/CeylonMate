# Copy these values into your shell or secret manager. Do not commit real secrets.
ConnectionStrings__CeylonMate=Host=localhost;Port=5432;Database=ceylonmate;Username=postgres;Password=change-me
Jwt__SigningKey=replace-with-at-least-32-random-bytes

# Development only. Setting Enabled=true creates one user per role.
SeedUsers__Enabled=false
SeedUsers__Password=replace-with-a-local-password
